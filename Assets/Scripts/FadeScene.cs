using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class FadeScene : MonoBehaviour
{/*
    public Animator anim;

   // public void Awake() => DontDestroyOnLoad(this.gameObject);

    // An animation event in the FadeOut Trigger will call OnFadeComplete()
    public void FadeToNextLevel() => anim.SetTrigger("FadeOut");

    public void OnFadeComplete() => SceneManager.LoadScene(SceneManager.GetActiveScene().buildIndex + 1);
 */
}
